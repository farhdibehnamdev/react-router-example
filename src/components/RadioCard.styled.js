import styled from "styled-components";

const StyledRadioCard = styled.div`
  padding: 2rem;
  font-size: 20px;
  font-weight: 700;
  box-shadow: 0 0 1rem rgba(0, 0, 0, 0.2);
  margin-bottom: 1rem;
  border-radius: 1rem;
`;

export default StyledRadioCard;
